import React from 'react';
import "../css/App.css";

const Header = props => (
  <header className="header">
    <img 
      className="img"
      src="../assets/transformers.jpg" 
      alt="Transformers Clicky Game!" />
  </header>
);

export default Header;